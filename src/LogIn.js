import './LogIn.scss';
import React from 'react';
import dotenv from 'dotenv';

// call the .env file
dotenv.config();

//allows the component to access all the properties and functions
class LogIn extends React.Component {
    handleSubmit = async (event) => {
        
    }

}